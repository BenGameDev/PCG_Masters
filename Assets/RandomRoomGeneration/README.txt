This project randomly generates rooms and assigns them room types.

Most of the constraints and values can be edited through the inspector however the main code for generating rooms is called RandomRoomGeneration.

Hold Tab to open the map during playtime of the scene and left mouse button click during playtime to regenerate the map, or just stop and start the scene again.